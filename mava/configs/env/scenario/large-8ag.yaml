# The config of the large-8ag environment
name: RobotWarehouse-v0
task_name: large-8ag

task_config:
  column_height: 8
  shelf_rows: 3
  shelf_columns: 5
  num_agents: 8
  sensor_range: 1
  request_queue_size: 8

env_kwargs:
  {}  # there are no scenario specific env_kwargs for this env
