# The config of the medium-4ag environment
name: RobotWarehouse-v0
task_name: medium-4ag

task_config:
  column_height: 8
  shelf_rows: 2
  shelf_columns: 5
  num_agents: 4
  sensor_range: 1
  request_queue_size: 4

env_kwargs:
  {}  # there are no scenario specific env_kwargs for this env
