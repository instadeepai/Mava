# The config of the 15x15-4p-3f scenario with the VectorObserver set as default

task_config:
  grid_size: 15
  fov : 15
  num_agents: 4
  num_food: 3
  max_agent_level: 2
  force_coop: False

# GridObserver config
grid_obs_config:
  grid_size: 15
  fov : 15
