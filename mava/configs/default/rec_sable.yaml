defaults:
  - logger: logger
  - arch: anakin
  - system: sable/rec_sable
  - network: rec_retention
  - env: craftax  # [cleaner, connector, gigastep, lbf, rware, smax]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
