defaults:
  - logger: logger
  - arch: anakin
  - system: mat/mat
  - network: transformer
  - env: lbf # [gigastep, lbf, mabrax, matrax, rware, smax]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
