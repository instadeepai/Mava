defaults:
  - logger: logger
  - arch: anakin
  - system: mat/mat
  - network: transformer
  - env: rware # [cleaner, connector, vector-connector, lbf, rware]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
