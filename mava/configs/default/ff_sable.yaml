defaults:
  - logger: logger
  - arch: anakin
  - system: sable/ff_sable
  - network: ff_retention
  - env: rware # [cleaner, connector, vector-connector, lbf, rware]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
