defaults:
  - logger: logger
  - arch: anakin
  - system: sable/ff_sable
  - network: retention
  - env: rware  # [cleaner, connector, gigastep, lbf, mabrax, matrax, rware, smax]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
