defaults:
  - logger: logger
  - arch: anakin
  - system: ppo/ff_mappo
  - network: mlp # [mlp, cnn]
  - env: rware # [cleaner, connector, vector-connector, lbf, rware]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
