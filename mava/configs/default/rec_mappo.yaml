defaults:
  - logger: logger
  - arch: anakin
  - system: ppo/rec_mappo
  - network: rnn # [rnn, rcnn]
  - env: rware # [cleaner, connector, vector-connector, lbf, rware]
  - _self_

hydra:
  searchpath:
    - file://mava/configs
