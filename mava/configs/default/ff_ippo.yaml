defaults:
  - logger: logger
  - arch: anakin
  - system: ppo/ff_ippo
  - network: mlp  # [mlp, continuous_mlp, cnn]
  - env: rware  # [cleaner, connector, gigastep, lbf, mabrax, matrax, rware, smax] # note: to test jumanji
  - _self_

hydra:
  searchpath:
    - file://mava/configs
